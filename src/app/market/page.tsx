import AswennaMarketplace from "@/components/AuctionItems";
import { FC } from "react";

type MarketplaceProps = {};

const Marketplace: FC<MarketplaceProps> = ({}) => {
  return <AswennaMarketplace />;
};

export default Marketplace;
